# Tell Travis CI we're using PHP
language: php

# Currently checking only against latest PHP - PHPCompatibilityWP does linting for earlier versions
php:
  - 7.3

before_script:
- composer install

script: ./vendor/bin/phpcs -ps cbox-openlab-core.php
